import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  columnWrapper: {
    gap: 10,
  },
  contentWrapper: {
    gap: 10,
    paddingTop: 16,
    paddingHorizontal: 16,
    paddingBottom: 32,
  },
});
