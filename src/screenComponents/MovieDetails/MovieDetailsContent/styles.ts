import {StyleSheet} from 'react-native';
import {colors} from '../../../colors';

export default StyleSheet.create({
  content: {
    padding: 16,
    gap: 10
  },
  sectionTitle: {
    color: colors.white,
    fontSize: 22,
    fontWeight: '700',
    marginBottom: 16,
  },
  description: {
    color: colors.white,
    fontSize: 16,
    lineHeight: 24,
  },
});
